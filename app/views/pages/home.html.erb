<div class="py-2">
  <h1 class="font-bold text-4xl">Vi Dash | AI Sounds Collections</h1>
  <p class="pb-4">Find and share sound clips generated by AI</p>

    <% if user_signed_in? %>
    <%= button_to "Sign Out", destroy_user_session_path, class: "bg-purple-500 text-purple-100 p-2 rounded-lg w-32 text-center",
      method: :delete  %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path, class: "bg-green-500 text-green-100 p-2 rounded-lg w-32 text-center" %>
    <% end %>
</div>
